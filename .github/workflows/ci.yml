name: CI

on:
  push:
  pull_request:

jobs:
  structural-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run structural checks
        run: |
          chmod +x check.sh
          ./check.sh
